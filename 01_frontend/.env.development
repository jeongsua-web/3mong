# 개발 모드 (`npm run dev`, `vite build --mode development`)
# 개발 서버. 별도 dev 서버가 없으면 운영 IP(3.35.22.100)로 바꿔 사용하세요.
VITE_API_BASE_URL=http://43.201.25.12:8080/api/v1

# 인증 우회. 커밋 값은 항상 false 유지.
# 로컬에서만 잠깐 우회하려면 .env.development.local 에 true 로 오버라이드하세요.
VITE_SKIP_AUTH=false
